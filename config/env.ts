// Configuración de variables de entorno
export const ENV = {
	// Contexto de la aplicación
	APP_CONTEXT: 'ssoma',

	// URL base de la API
	API_BASE_URL: 'https://pas.oke-mty-tst.splata.penoles.mx/v1/api',

	// Endpoints de autenticación
	AUTH_LOGIN_ENDPOINT: '/auth/login',
} as const; 